services:
  - type: web
    name: primeace-tech
    runtime: node
    buildCommand: bun install && bun run build
    startCommand: bun run dist/server/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
